# Docker环境配置文件
# 用于Docker Compose环境的配置

# 服务器配置
SERVER:
  host: 0.0.0.0
  port: 8000
  debug: false

# SQLite 数据库(单文件,挂载到 /app/data 持久化)
SQLITE:
  path: data/vibe_cook.db

# JWT认证配置
JWT:
  secret_key: "change-me-in-production-use-a-random-string"
  algorithm: HS256
  expire_minutes: 10080  # 7天
