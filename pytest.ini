[pytest]
# pytest配置文件

# 测试文件匹配模式
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 输出选项
addopts =
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# 测试路径
testpaths = tests

# 标记
markers =
    slow: 标记慢速测试
    concurrent: 标记并发测试
    integration: 标记集成测试
